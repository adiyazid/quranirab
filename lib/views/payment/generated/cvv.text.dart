import 'package:flutter/material.dart';
import 'package:nb_utils/nb_utils.dart';
import 'package:provider/provider.dart';

import '../../../provider/card.provider.dart';
import '../../../theme/theme_provider.dart';

/* Text CVV
    Autogenerated by FlutLab FTF Generator
  */
class CVVText extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    final theme = Provider.of<ThemeProvider>(context);
    return Scaffold(
      body: Center(
        child: TextFormField(
            cursorColor: Colors.white,
            onChanged: (v) {
              Provider.of<CardProvider>(context, listen: false).setCvv(v);
            },
            decoration: InputDecoration(
                suffix: Consumer<CardProvider>(builder: (context, card, _) {
                  if (card.cvvResults != null &&
                      card.cvvResults!.isPotentiallyValid) {
                    return Icon(Icons.check);
                  } else {
                    return Icon(Icons.close);
                  }
                }),
                border: InputBorder.none,
                filled: true,
                fillColor: theme.isDarkMode ? Color(0xff67748E) : Colors.white,
                labelText: ('CVV'),
                labelStyle:
                    TextStyle(color: theme.isDarkMode ? white : Colors.black)),
            style: TextStyle(color: theme.isDarkMode ? white : Colors.black)),
      ),
    );
  }
}
