import 'package:flutter/material.dart';
import 'package:nb_utils/nb_utils.dart';
import 'package:provider/provider.dart';

import '../../../provider/card.provider.dart';
import '../../../theme/theme_provider.dart';

/* Text 1234 1234 1234 1234
    Autogenerated by FlutLab FTF Generator
  */
class HintTextCardNumber extends StatelessWidget {
  const HintTextCardNumber({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    final theme = Provider.of<ThemeProvider>(context);
    return Scaffold(
      body: Center(
        child: TextFormField(
            keyboardType: TextInputType.numberWithOptions(decimal: false),
            cursorColor: Colors.white,
            onChanged: (v) {
              Provider.of<CardProvider>(context, listen: false).setCard(v);
            },
            decoration: InputDecoration(
                suffix: Consumer<CardProvider>(builder: (context, card, _) {
                  if (card.cvv != null) {
                    card.setCvv(card.cvv!);
                  }
                  if (card.ccNumResults != null &&
                      card.ccNumResults!.isPotentiallyValid) {
                    return Icon(Icons.check);
                  } else {
                    return Icon(Icons.close);
                  }
                }),
                border: InputBorder.none,
                filled: true,
                fillColor: theme.isDarkMode ? Color(0xff67748E) : Colors.white,
                labelText: ('Card Number'),
                labelStyle:
                    TextStyle(color: theme.isDarkMode ? white : Colors.black)),
            style: TextStyle(color: theme.isDarkMode ? white : Colors.black)),
      ),
    );
  }
}
